{
  "name": "council-meetings",
  "version": "1.0.0",
  "description": "Making Dublin City Council meetings available as podcasts.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npx @11ty/eleventy --serve --port=8002",
    "build": "node index.js",
    "fetch": "node fetch.js",
    "dev": "npm run fetch && npm run serve"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy-plugin-rss": "^1.2.0",
    "@supabase/supabase-js": "^2.39.7",
    "aws-sdk": "^2.866.0",
    "dotenv": "^8.2.0",
    "fluent-ffmpeg": "^2.1.2",
    "node-fetch": "^2.6.1",
    "podcast": "^1.3.0",
    "rss-parser": "^3.12.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
